// Container specifications for the Docker profile

process {

    // Container from Docker Hub
    container = 'vmikk/nextits:1.2.0'

    //// Container from Quay.io registry
    // container = 'docker://quay.io/vmikk/nextits:1.1.0'

}

